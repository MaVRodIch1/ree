# Telegram session files — keep these local, never commit
sessions/*.session
sessions/*.session-journal
new_sessions/*.session
new_sessions/*.session-journal

# tdata source folders and zip imports
tdata_accounts/*/
tdata_zips/*.zip
tdata_zips/_extract_*/

# avatar photos for warming (keep local)
avatars/*.jpg
avatars/*.jpeg
avatars/*.png
avatars/*.webp
avatars/_used/

# working name/username pools — consumed locally, seeded from *.example.txt
nicknames.txt
names.txt

# Split API key — NEVER commit
split_api_key.txt

# logs
claims.log

# python
__pycache__/
*.pyc
venv/
venv2/
.venv/

# generated Tonkeeper top-up link
topup_link.txt

# asteroid unclaimable accounts skip-list
asteroid_skip.txt

# sniper audio files
sniper_audio/*
!sniper_audio/.gitkeep

# farming progress state
progress.json

# sniper session (kept out of farm cycles)
sniper_session/*.session
sniper_session/*.session-journal

# MEGA links and extracted 2FA passwords
mega_links.txt
mega_2fa.txt
_tmp_mega/
mega_failed.txt

mega_tdata/
convert_failed.txt

# TON wallets — seed phrases, NEVER commit
wallets.json
wallets_backup.txt

# Six Seven token-drop log
sixseven_drops.txt

# Leaderboard tracker score history
top_history.json

# Yandex.Disk import
yandex_links.txt
yandex_2fa.txt
yandex_failed.txt
yandex_tmp/

# Leaderboard daily-recap state
top_daily.txt

# Leaderboard PvP cache
top_pvp_state.json

# Announced-tasks memory (which task templates were already posted)
top_tasks_seen.json
